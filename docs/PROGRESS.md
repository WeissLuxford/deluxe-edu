# Что делаем сейчас

Рабочий журнал, не архитектурные решения — те в `DECISIONS.md`. Точки по
3-10 слов, чтобы восстановить контекст после `/clear` за секунды.

---

## Сейчас в фокусе

- Регистрация закрыта: все три способа доведены до рабочего состояния
- Дальше — проверка боевого домена и переменных на Vercel

## Сделано (эта сессия)

- Способ 1 — Google OAuth: ключи заданы, флаг включён
- Способ 2 — телефон/SMS: Eskiz в test mode, код печатается в консоль
- Способ 3 — почта: SMTP Brevo заведён, соединение и авторизация проходят
- `NEXT_PUBLIC_EMAIL_ENABLED=true` — способ «по почте» показывается
- `scripts/check-smtp.mjs` — диагностика SMTP без отправки письма
- Прод-база подключена, `prisma migrate status` — схема актуальна
- `npx tsc --noEmit` и `npm run build` проходят чисто

## Дальше

- Задать переменные в Vercel и пересобрать (список ниже)
- `ESKIZ_TEST_MODE=false` на проде, иначе людям придёт тестовая строка
- `TURNSTILE_SECRET_KEY` на проде обязателен, иначе формы отказывают
- `NEXTAUTH_URL=https://highgate.uz`, локальный адрес ломает ссылки в письмах
- `NEXT_PUBLIC_*` вшиваются при сборке — добавить и пересобрать
- Redirect URI в Google Cloud Console для боевого домена
- Пройти регистрацию всеми тремя способами на проде руками
- `scripts/peek-users.mjs` — временный, удалить после проверок
