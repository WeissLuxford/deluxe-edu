// The site's multi-color identity, used wherever a single brand color would
// read as flat — logotype letters, decorative icons, per-item accents.
// CSS variable references so dark/light theme swaps still apply correctly.
export const PLAYFUL_PALETTE = [
  'var(--brand)',
  'var(--accent-blue)',
  'var(--accent-green)',
  'var(--accent-violet)',
  'var(--accent-amber)'
]
