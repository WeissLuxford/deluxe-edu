-- Адрес урока уникален внутри курса.
-- Раньше это проверялось только кодом админки, из-за чего повторный
-- запуск сида плодил дубли уроков.
CREATE UNIQUE INDEX "Lesson_courseId_slug_key" ON "Lesson"("courseId", "slug");
